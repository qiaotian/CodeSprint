/**
Given a matrix in which each row and each column is sorted, write a method to find whether an element is in it.

Greedy method doesn't work
*/

bool search_in_2d_sorted_array(vector<vector>& mat, int taget) {
    
    return false;
}
